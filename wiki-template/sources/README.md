# sources/

Raw, immutable inputs: notes, articles, transcripts, exported threads — anything
worth remembering. One file per source, stable filename/slug.

**READ-ONLY once written.** Never edit or delete a source file — if it turns out to
be wrong, add a note in the page that cites it, don't rewrite history here. See
[../CONVENTIONS.md](../CONVENTIONS.md).
