# Personal Wiki

Your compounding personal knowledge base, maintained under the LLM-wiki pattern.
Conventions & operations (ingest / query / lint): see [CONVENTIONS.md](CONVENTIONS.md).
Page frontmatter schema: see [frontmatter-schema.md](frontmatter-schema.md).

**This index is the only page meant to be auto-loaded every session — keep it lean.**

## Pages

(none yet — pages land here as you use this wiki)

## Layout
- `sources/` — raw, immutable inputs (read-only)
- `pages/` — wiki pages (LLM-owned; one topic per file; cite sources)
- `log/` — dated activity log (`YYYY-MM-DD.md`)
